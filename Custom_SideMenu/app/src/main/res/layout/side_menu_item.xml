<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/menu_item_wrapper"
    android:layout_width="match_parent"
    android:layout_height="50dp"
    android:background="@color/white">

    <TextView
        android:id="@+id/menu_txt"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_toStartOf="@id/menu_arrow_btn"
        android:gravity="start|center"
        android:letterSpacing="-0.05"
        android:paddingStart="@dimen/side_nav_padding"
        android:paddingEnd="@dimen/side_nav_padding"
        android:textSize="18sp"
        tools:text="item" />

    <ImageView
        android:id="@+id/menu_arrow_btn"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:background="@android:color/transparent"
        android:src="@drawable/navigate_next" />
</RelativeLayout>